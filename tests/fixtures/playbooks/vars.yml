- name: Variables Playbook
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Print variable
      debug:
        msg: "The greeting is: {{ greeting | default('default_greeting') }}"
